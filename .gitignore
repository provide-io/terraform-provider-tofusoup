# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# FlavorPack
*.psp
dist/terraform-provider-*

# MkDocs
site/

# Provide (track .md files, ignore generated)
.provide/foundry/
.provide/cache/
.provide/tmp/

# OS
.DS_Store
Thumbs.db
.soup/
.soup/**
*.log
.mypy_cache/
.ruff_cache/

# Local act CI config (machine-specific)
.actrc

# Built release artifacts (out of scope for git)
release/

# Built release artifacts (out of scope for git)
terraform-provider-*_v*

# flavorpack generates a fresh signing keypair per build (see
# flavor/packaging/keys.py); nothing verifies against a committed one. They are
# build artifacts, and committing them is how private keys reached five public
# repositories in this suite.
keys/
*.private-key.pem
