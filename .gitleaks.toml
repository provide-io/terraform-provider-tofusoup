# Gitleaks configuration
# Test signing keys - intentionally committed
# Auto-generated by wrknv security.generate
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

[[allowlists]]
description = "Test signing keys - intentionally committed"
paths = [
    '''keys/.*$''',
]
